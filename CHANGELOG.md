# Tix 更新日志

## v2.0.0 - 2026-03-11

### 🚀 重大更新

**数据库优化**
- 连接池配置（最大25连接，空闲10连接）
- WAL模式提升并发性能
- PRAGMA优化（缓存、超时、内存临时表）
- 新增数据库索引（发起人、优先级、复合索引）

**优先级功能**
- 新增优先级字段（1低/2普通/3高/4紧急）
- 前端优先级筛选器
- 工单卡片优先级颜色标识（左边框）
- 创建/编辑工单可选优先级

**批量操作API**
- `POST /v1/tickets/batch-delete` 批量删除
- `POST /v1/tickets/batch-update` 批量更新

**新增API**
- `GET /v1/export/csv` CSV格式导出
- `GET /v1/stats` 统计数据
- `GET /v1/initiators` 发起人列表
- `GET /v1/system/info` 系统信息

**搜索优化**
- 支持标题/内容/发起人模糊搜索
- `search` 参数替代原来的 `initiator`

**配置文件**
- 首次启动自动创建 `~/.tix/config.yaml`
- 配置保存时自动创建目录

**前端改进**
- 优先级显示标签（带颜色区分）
- 表单布局优化（发起人+优先级并排）

### 📦 其他改进
- 代码结构优化，类型统一到 model 包
- 移除自动打开浏览器（避免卡顿）
- 项目重命名为 "Tix"

---

## v1.0.0 - 2026-03-11

### PDF报告导出
- **一键导出**: 左侧筛选面板新增"导出PDF报告"按钮
- **时间范围**: 根据当前筛选的开始/结束日期导出
- **统计概览**: 工单总数、完成率、分类统计（带进度条可视化）
- **图表展示**: 饼图（分类分布）+ 柱状图（每日工单趋势）
- **工单明细**: 卡片式布局，显示标题、内容、处理过程，自动换行
- **报告标题**: 格式为"XXXX年X月工作报告"

### PDF字体配置
- **字体设置**: 设置页面新增"PDF设置"标签页
- **自动检测**: 根据操作系统自动推荐字体路径
- **跨平台支持**: Windows/macOS/Linux 常用中文字体路径提示
- **配置持久化**: 字体路径保存到config.yaml

### 新用户体验优化
- **友好错误提示**: 创建工单时，如果没有分类，提示"请先在设置中创建分类"
- **AI未配置处理**: 如果未配置AI，自动选择第一个分类，而不是报错
- **错误信息展示**: 前端正确显示后端返回的错误消息，不再只显示"操作失败"

## 2026-03-11

### 页面布局优化
- **左侧筛选面板**: 筛选条件（搜索、分类、状态、日期）移至左侧固定面板
- **容器加宽**: 从 `max-w-5xl` 改为 `max-w-7xl`，更好利用屏幕空间
- **sticky定位**: 左侧面板随滚动固定
- **清除筛选**: 一键清除所有筛选条件

### 时间筛选功能
- **按日期筛选**: 新增开始日期和结束日期筛选
- **后端支持**: API支持 `start_date` 和 `end_date` 参数
- **数据库查询**: `ListOptions` 增加 `StartDate` 和 `EndDate` 字段

### 编辑工单时间
- **创建时间可编辑**: 编辑工单时可修改创建时间
- **完成时间可编辑**: 编辑工单时可修改完成时间（补录历史工单场景）
- **前端datetime-local**: 使用原生日期时间选择器

### 分类持久化修复
- **同步到配置**: 添加分类时同步更新 `config.yaml`
- **修复丢失问题**: 解决添加分类后重启丢失的问题

### 默认分类调整
- **新用户空分类**: `DefaultConfig()` 默认无分类，让用户新建
- **老用户保留**: 现有 `config.yaml` 中的分类不受影响

### 启动Banner修复
- **中文字符宽度**: 正确计算CJK字符宽度（2格），其他符号1格
- **边框对齐**: 解决动态内容导致右边框错位的问题

### 思源推送覆盖优化
- **搜索已有文档**: 使用 `fullTextSearchBlock` API 查找同名文档
- **更新而非创建**: 已存在则用 `updateBlock` 更新内容，避免重复
- **日志区分**: 区分"创建"和"更新"两种操作的日志输出

### 启动优化
- **控制台日志中文化**: 所有日志输出改为中文
- **启动Banner**: 显示美观的系统信息框
- **自动打开浏览器**: 服务启动后自动打开前端页面
- **可点击地址**: 终端中显示完整URL，支持Ctrl+点击打开

### 分类管理
- **新增分类管理标签页**: 设置页面新增分类管理功能
- **添加分类**: 输入名称添加新分类
- **编辑分类**: 直接重命名分类，所有工单自动关联新名称
- **删除分类**:
  - 无工单分类：直接删除
  - 有工单分类：选择转移目标后删除
- **显示工单数量**: 每个分类显示关联的工单数量

### Bug修复
- **编辑工单分类**: 修复编辑工单时修改分类不生效的问题
- **多选框逻辑**: 修复只点checkbox不触发批量操作菜单的问题

### 思源笔记配置化
- **可配置API地址**: 支持自定义思源API地址
- **可配置笔记本ID**: 在设置中配置目标笔记本
- **测试连接**: 验证思源是否运行
- **配置持久化**: 保存到config.yaml

### 导入导出优化
- **导出包含配置**: 导出JSON包含AI配置、思源配置和分类设置
- **导入恢复配置**: 导入时自动恢复AI配置和思源配置
- **错误提示优化**: 导入失败时显示具体错误信息

### 设置页面标签页布局
- **顶部标签页**: 四个标签按钮（AI配置、分类管理、数据管理、思源笔记）
- **点击切换**: 点击标签切换到对应设置内容
- **关闭按钮**: 右上角 × 关闭设置窗口

### 新建工单优化
- **分类默认AI选择**: 下拉框默认为空，由AI自动选择
- **防止重复提交**: 保存按钮点击后禁用，显示"处理中..."

### 思源推送优化
- **检查思源状态**: 推送前检查思源是否运行，未运行返回503错误
- **空工单检查**: 没有工单时提示错误
- **未配置提示**: 未配置笔记本ID时提示先配置

### AI智能分类
- **AI自动选择分类**: 根据工单内容自动选择合适的分类
- **分类可选**: 表单中分类可留空，由AI自动选择

### AI配置界面
- **前端设置页面**: 添加AI配置表单（API密钥、地址、模型）
- **API接口**: 
  - `GET /v1/config/ai` - 获取AI配置
  - `POST /v1/config/ai` - 保存AI配置
  - `POST /v1/config/ai/test` - 测试AI连接
- **配置持久化**: 保存到 `config.yaml` 文件

### AI标题生成功能
- **新增AI集成**: 支持OpenAI标准API自动生成工单标题
- **默认行为**: 未配置AI时截取内容前5字作为标题
- **数据库**: 添加 `title` 字段

### 推送思源格式优化
- **文件名标准化**: 改为 `2026年3月` 格式
- **去掉重复标题**: 文档名即标题，内容不再写 `# 2026年3月IT工单记录`
- **使用工单标题**: 推送时使用AI生成的标题字段
- **覆盖写入**: 推送前先删除同名文档，避免重复
- **修复中文乱码**: HTTP请求添加 `charset=utf-8` 头

### 分类调整
- 新分类: 硬件故障、网络问题、软件支持、会议设备

### 推送格式
```markdown
> 此文档由IT工单系统自动生成，仅用于展示和报告，不可反向导入。

---

## 3月11日 星期二

### 001 办公室WiFi信号弱

| 发起人 | 分类 | 状态 | 创建时间 | 完成时间 |
|--------|------|------|----------|----------|
| 王五 | 网络问题 | ⏳ 处理中 | 2026-03-11 08:30:00 | |

**问题描述**:
办公室WiFi信号弱，网速慢
```

---

## 2026-03-10

### 初始版本
- 后端API: 创建/列表/详情/更新/删除/分类查询
- 前端页面: Tailwind CSS、多选、批量操作、深色模式
- 导入导出: JSON格式
- 推送思源: 按月份分组推送
- 单二进制部署: embed静态文件
- 端口自适应: 8080起
