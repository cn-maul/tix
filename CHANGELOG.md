# Tix 更新日志

## v3.1.0 - 2026-03-16

### ✅ 滑块验证码集成

- 集成 `tianai-captcha-go` 滑块验证码，替换短信验证码
- 公开工单提交页面现使用滑块验证，无需短信服务

### 🖥️ 登录系统重构

- 首次启动进入管理员注册流程；系统完成初始化后仅展示登录入口
- 独立登录页面，统一为 Sober UI 风格
- 登录框宽度优化为 `720px`
- 登录与注册主按钮改为并排显示
- 登录页新增深色/浅色主题切换按钮
- 新增账户管理设置页面，管理员可创建和删除用户

### 🌐 公开工单提交页

- 新增 `public.html` 公开入口，无需登录即可访问
- 新增 `POST /v1/public/tickets` 公开提交 API，并补充公开分类接口
- 表单采用 Sober 组件库，支持问题描述、分类选择、联系方式（可选）
- 页面优先适配移动端，兼顾 `375px` 手机宽度与桌面扫码打开场景
- 提交成功后展示确认信息与工单编号

## v3.0.4-dev - 2026-03-16

### 🔐 账号系统

**数据库与数据模型**
- 新增 `users` 表（`id/username/password_hash/role/created_at/last_login_at`）
- 新增 `sessions` 表（基于 `token_hash` 的会话存储，含过期时间）
- `tickets` 表新增 `owner_id` 字段，并补充对应索引
- `Ticket` 模型新增 `owner_id`，用于数据归属隔离
- 新增 `User` 模型与登录/注册请求响应模型

**权限与鉴权基础设施**
- 新增 `internal/auth/context.go`：请求上下文注入/读取当前用户、管理员判断
- 新增 `internal/auth/token.go`：随机 token 生成与 token hash 计算
- 新增中间件 `middleware.Auth(db)`：
  - 对 `/v1/**` 启用 Bearer Token 校验
  - 自动清理过期会话
  - 解析会话后将用户注入请求上下文
  - 对 `/v1/config*`、`/v1/system*`、`/v1/users*` 启用管理员权限限制
- `main.go` 已接入 `Auth` 中间件

**后端接口（已新增）**
- `POST /v1/auth/register`：仅允许首次启动时创建首个管理员（bootstrap）
- `POST /v1/auth/login`：账号密码登录，返回 token + user
- `GET /v1/auth/me`：获取当前登录用户
- `POST /v1/auth/logout`：退出并删除当前 token 对应会话
- `POST /v1/users`：管理员创建普通用户账号
- 路由注册与 `Handler` 结构已接入数据库实例

**工单数据隔离（后端已接入）**
- `CreateTicket` 自动写入当前用户 `owner_id`
- 查询/统计/更新/删除/批量操作默认按登录用户进行 owner 级过滤
- 管理员角色保留全量可见与全量操作能力

### 🖥️ 前端登录与管理

- 页面启动时先校验登录态，未登录则显示登录/首个管理员初始化弹窗
- 前端请求统一通过带 `Authorization: Bearer <token>` 的 `apiFetch`
- 顶部栏新增当前用户展示与退出登录
- 管理员才显示“设置”入口和“用户管理”页签
- 新增最小可用的管理员建号表单，用于创建普通处理人员账号
- 报告下载改为带鉴权的 `fetch + blob`，避免 `<a>` 直链无法携带 token

### 📚 文档

- README 增加首次登录、管理员/普通账号权限说明
- API 列表补充认证与用户管理接口

### 🧪 验证状态

- 已执行 `go build ./...`，后端编译通过
- 已补充 `golang.org/x/crypto/bcrypt` 依赖
- 本次未做浏览器端人工点击回归，前端联调仍建议在实际页面中再跑一遍

## v3.0.3 - 2026-03-16

### 🔧 后端优化

**API 安全与规范**
- 限制工单列表 `sort` 只允许白名单字段，修复 `ORDER BY` 注入风险
- 单条工单更新/删除检查 `RowsAffected`，不存在资源时返回 `404`
- 主要 JSON 写接口启用严格解码，未知字段直接返回 `400`
- 思源配置测试接口增加上游 HTTP 状态码检查
- `go.mod` 同步到 `go 1.26`

**配置与并发安全**
- `Recover` / `Logger` / `CORS` 中间件正式接入 HTTP 服务入口
- `Handler` 增加 `RWMutex`，统一保护运行时配置和分类列表的并发读写
- 配置读取改为快照，配置写入改为统一加锁保存，减少并发请求下的数据竞争风险

**性能优化**
- 导入逻辑从逐条查重改为事务内批量 `INSERT OR IGNORE`
- 去掉导入时的 N+1 查询，提升大批量导入吞吐

**敏感信息处理**
- 导出配置时不再包含 AI `api_key` 明文，仅保留是否已配置和非敏感字段

### 💄 界面优化

**Sober 组件统一**
- 顶部操作按钮、设置页标签切换按钮、分类管理操作按钮统一改为 Sober 组件（`s-icon-button` / `s-button`）
- 创建/编辑弹窗关闭按钮改为 `s-icon-button`，减少原生按钮与组件混用
- 设置页侧栏与内容区样式重排，优化标题、说明文字、按钮区与状态文字的位置层级

**设置页布局优化**
- 调整设置弹窗尺寸与侧栏宽度，提升信息密度和可读性
- 为各设置面板增加统一标题区（标题 + 描述），减少文案拥挤感
- 统一卡片、表单分区、状态提示、按钮间距；移动端下按钮改为纵向布局
- 修复部分提示文案样式不一致（去除无效 Tailwind 类，改为项目内样式类）

### 🐛 Bug 修复

**Sober 用法修正**
- `s-text-field` 多行输入统一改为 `type="multiline"`，并使用 `max-length` 属性
- `s-picker` 的读取/回填逻辑统一使用 `value`（新增 `setPickerValue()`），减少依赖手动 `selected`/`label` 同步
- 工单勾选框 `checked` 属性改为布尔写法，避免字符串值导致的兼容问题

**交互与安全**
- 深色模式图标切换改为显式 JS 控制，避免依赖无效类名造成显示异常
- Snackbar 消息赋值从 `innerHTML` 改为 `textContent`，降低注入风险
- 分类管理按钮的分类名传参改为 `encodeURIComponent/decodeURIComponent`，避免引号等特殊字符导致点击异常

### ⏭️ 后续计划

**准备继续处理**
- 为数据库操作和外部 HTTP 调用补齐 `context.Context` 传递，统一超时与取消链路
- 复用 AI / 思源 HTTP client，减少重复创建连接
- 优化导出 / PDF 报告 / 思源推送的大数据路径，避免一次性全量载入内存
- 继续审查列表查询与统计接口的索引命中情况，评估是否需要增加复合索引或引入 FTS

**记录时间**
- 后端优化与计划补充时间：2026-03-16 10:49:34 CST

### ✅ 本次继续推进 - 2026-03-16

**context 取消链路落地**
- `Handler -> Service -> Database` 主链路改为显式传递 `r.Context()`
- 数据库查询/写入统一切换为 `ExecContext` / `QueryContext` / `QueryRowContext` / `BeginTx`
- 报告导出、导入、列表、统计、批量操作等接口支持请求级取消

**HTTP client 复用**
- `Handler` 内新增复用 `http.Client`（含超时），用于 AI 分类/测试与思源测试/推送
- `AIService` 支持注入共享 `http.Client`，移除每次调用临时创建 client 的模式
- 思源推送改为 `NewRequestWithContext`，请求超时和取消可沿链路生效

### ✅ 本次补丁修复 - 2026-03-16

**编辑工单兼容修复**
- 修复编辑保存返回 `INVALID_JSON`：`UpdateTicketReq` 补充 `initiator/title/content` 字段
- 后端 `PATCH /v1/tickets/{id}` 增加对应字段更新与校验，兼容当前前端提交结构

**导入后筛选面板分类同步**
- 修复导入后“设置里可见、编辑可见、筛选面板不可见”的问题
- 前端导入成功后新增 `await loadCategories()`，再刷新工单列表

**性能说明**
- 本次未新增独立压测结果；性能收益仍主要来自前面已落地的导入事务化与批量 `INSERT OR IGNORE`
- 在大批量导入场景下，吞吐优于逐条查重写入路径

---

## v3.0.1 - 2026-03-13

### 🐛 Bug 修复

**导入导出**
- 修复导入 API 无法解析 FormData 上传的 JSON 文件
- 修复导入配置后分类在设置中可见，但在新建/编辑工单和筛选面板中不可见的问题
- 导出时包含完整配置（分类、AI、思源、PDF字体）

**静态资源**
- 本地化 sober.min.js，解决 CDN 加载失败问题

### ✨ 新功能

**排序按钮**
- 新增排序切换按钮（最新优先/最早优先）
- 排序状态保存到 localStorage，刷新页面保持

---

## v3.0.0 - 2026-03-12

### 🎨 UI 重构 - Sober 组件库

**全面迁移到 Sober (Material You 设计风格)**
- 使用 Sober Web Components 替换原有 Tailwind CSS 实现
- 统一的设计语言和交互体验
- 更好的可访问性和触摸支持

**组件替换**
- `<s-button>` - 填充、轮廓、文字等多种按钮样式
- `<s-text-field>` - 输入框、多行文本
- `<s-picker>` - 下拉选择器
- `<s-checkbox>` - 复选框
- `<s-date-picker>` - 日期选择器
- `<s-snackbar>` - 消息通知
- `<s-chip>` - 标签徽章

**主题系统**
- 亮色/暗色模式切换
- 灰色主色调（替代原来的蓝色）
- CSS 变量驱动的主题系统
- 跟随系统主题偏好

### 🐛 Bug 修复

**筛选功能**
- 修复左侧筛选条件变化后工单列表不更新的问题
- 修复 Web Components 事件监听问题（使用 addEventListener 替代 onchange 属性）
- 修复日期选择器值获取问题
- 新增搜索框防抖（300ms）

**AI 配置**
- 修复保存 AI 配置后不生效的问题（动态创建 AI 服务）
- 修复清空 AI 配置后保存仍保留的问题（使用指针区分空值和未发送）
- 脱敏 API Key 不再覆盖原值

**工单编辑**
- 修复新建工单默认值问题（分类、优先级）
- 修复编辑工单时选择器值不显示的问题
- 修复保存后批量操作菜单不消失的问题

**通知系统**
- 修复 Snackbar 显示空白的问题
- 使用 `innerHTML` 设置消息内容
- 统一使用 `showToast` 函数

**暗色模式**
- 修复 s-picker 下拉菜单在暗色模式下显示白色背景的问题
- 修复下拉菜单项文字在暗色模式下不可见的问题
- 添加 `--md-sys-color-surface-container` CSS 变量支持

### 💄 界面优化

**筛选面板**
- 下拉选择器使用 `label` 属性显示当前选中值
- 日期选择器样式优化
- 清除筛选按钮重置所有值

**设置页面**
- 关闭按钮改为文字按钮
- 保存/测试连接统一使用 Snackbar 通知

### 🔧 技术改进

**前端**
- 新增 `getPickerValue()`、`getInputValue()`、`getDateValue()` 辅助函数
- 统一 Web Components 值获取逻辑
- 清理调试日志

**后端**
- AI 服务动态创建，使用最新配置
- 详细日志输出便于调试

---

## v2.0.2 - 2026-03-11

### 🐛 Bug修复

**AI分类**
- 修复未配置AI时选择"AI自动分类"不报错的问题
- 未配置AI时提示"请先在设置中配置AI，或手动选择分类"
- 修复AI分类选择逻辑未实现的问题

**配置文件**
- 统一配置文件位置到软件根目录 `./config.yaml`
- 移除 `~/.tix/` 用户目录逻辑

**前端提示**
- 修复保存AI/PDF/思源配置时判断 `result.success` 不存在的问题
- 改用 `res.ok` 判断HTTP状态
- 修复测试连接显示 `[object Object]` 的问题
- 修复加载AI配置时判断 `data.enabled` 不存在的问题

### 💄 界面优化

**设置页面**
- 重构为左侧侧边栏布局，标签页竖向排列
- 去掉设置标签前的表情符号，让文字排成一行
- 激活标签使用蓝色高亮背景

---

## v2.0.1 - 2026-03-11

### 🐛 Bug修复

**分类管理**
- 修复分类列表显示 undefined（前端期望对象但后端返回数组）
- 修复添加/编辑/删除分类的响应判断逻辑
- 修复编辑工单时分类下拉列表为空

**配置文件**
- 修复首次启动配置文件保存失败（目录不存在）
- 配置保存时自动创建 `~/.tix/` 目录

**工单编辑**
- 修复编辑工单时分类字段无法保存
- UpdateTicket 允许空分类（保持原分类不变）
- openEditModal 确保分类列表已加载

**前端兼容性**
- loadCategories 兼容数组和对象两种格式
- 修复残留代码导致的JS语法错误

---

## v2.0.0 - 2026-03-11

### 🚀 重大更新

**数据库优化**
- 连接池配置（最大25连接，空闲10连接）
- WAL模式提升并发性能
- PRAGMA优化（缓存、超时、内存临时表）
- 新增数据库索引（发起人、优先级、复合索引）

**优先级功能**
- 新增优先级字段（1低/2普通/3高/4紧急）
- 前端优先级筛选器
- 工单卡片优先级颜色标识（左边框）
- 创建/编辑工单可选优先级

**批量操作API**
- `POST /v1/tickets/batch-delete` 批量删除
- `POST /v1/tickets/batch-update` 批量更新

**新增API**
- `GET /v1/export/csv` CSV格式导出
- `GET /v1/stats` 统计数据
- `GET /v1/initiators` 发起人列表
- `GET /v1/system/info` 系统信息

**搜索优化**
- 支持标题/内容/发起人模糊搜索
- `search` 参数替代原来的 `initiator`

**配置文件**
- 首次启动自动创建 `config.yaml`
- 配置保存时自动创建目录

**前端改进**
- 优先级显示标签（带颜色区分）
- 表单布局优化（发起人+优先级并排）

### 📦 其他改进
- 代码结构优化，类型统一到 model 包
- 移除自动打开浏览器（避免卡顿）
- 项目重命名为 "Tix"

---

## v1.0.0 - 2026-03-11

### PDF报告导出
- **一键导出**: 左侧筛选面板新增"导出PDF报告"按钮
- **时间范围**: 根据当前筛选的开始/结束日期导出
- **统计概览**: 工单总数、完成率、分类统计（带进度条可视化）
- **图表展示**: 饼图（分类分布）+ 柱状图（每日工单趋势）
- **工单明细**: 卡片式布局，显示标题、内容、处理过程
- **报告标题**: 格式为"XXXX年X月工作报告"

### PDF字体配置
- **字体设置**: 设置页面新增"PDF设置"标签页
- **自动检测**: 根据操作系统自动推荐字体路径
- **跨平台支持**: Windows/macOS/Linux 常用中文字体路径提示

### 思源笔记集成
- **推送工单**: 将工单按月份推送到思源笔记
- **Markdown格式**: 自动生成格式化的Markdown文档

### AI智能功能
- **AI自动分类**: 根据工单内容自动选择合适的分类
- **AI标题生成**: 自动生成工单标题

### 数据管理
- **导入导出**: JSON格式导入导出
- **分类管理**: 添加、编辑、删除分类
